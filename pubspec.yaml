name: financas_inteligentes
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.5.0
  cloud_firestore: ^6.1.3
  firebase_auth: ^6.2.0
  fl_chart: ^1.1.1
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  http: ^1.2.2
  flutter_secure_storage: ^10.0.0
  csv: ^6.0.0
  file_picker: ^10.3.10
  cupertino_icons: ^1.0.8
  logger: ^2.2.0
  flutter_masked_text2: ^0.9.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  provider: ^6.1.2
  # Notificacoes locais agendadas
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  # Geracao e exportacao de relatorios PDF
  pdf: ^3.11.1
  printing: ^5.13.2
  # Login social
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
